You are an Google web developer. You have an access to an api whos credentials are in the env file and stuctural info is in @openapi.json. I need to build an fully fledged order management system. There are different steps that needs to be done. Users log in to the site and then makes on the home page he should be able to see different store card and each store will have its own order lists. on the top right corner area i need an option for create new order list,logout. When user selects create new order list he/she needs to select the store and date. after he selects it it should take to the order list of for that store for that particular date. After that user will move in the store and scan using barcode scanner/or using the mobile phone camera / using search bar will enter name(partial should show suggestions,needs to show most similar, needs to omit ' and all ) / product number of the items that needs to be ordered. as soon as the person adds item using camera or scanner or search it should show its current stock of that product in a box  and a box for its on shelf quantity. and and option to cancel or add item to list. when the users adds it. It needs to be added in the sheet of that store.I will also calculate each how much each product is needed each month for a store. you need to show the entire list using the library or sheets server. then from the list there should be options to edit the  on shelf quantity delete the row. on the top there should be option to show other store stocks and their monthly needed. and there will be a column for transfer from where the user can select the store to tranfer that item from. there will be option on the top of the list to download/print transfer list in pdf /excel format. then also an option to download the entire list in pdf/excel format in the downloaded pdf it dont need to show the other store stocks on the product name,number,quantity,transfer from  columns. admin should be able to create users . All the stores and priduct numbers will be fetched from the korona pos directly we can save the products  to keep the search easy but then we need to do it every certain hours fetch and see if new things are added in the system or not.  You also need to find a smart way to monthly needed for each product and that number needs to be upto date with the pos system same for the stock. 